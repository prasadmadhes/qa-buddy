[project]
name = "qa-buddy"
version = "0.1.0"
description = "AI-powered QA assistant that diagnoses test failures"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111",
    "uvicorn>=0.30",
]
